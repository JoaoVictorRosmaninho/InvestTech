<%= form_with(model: security) do |form| %>
  <% if security.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(security.errors.count, "error") %> prohibited this security from being saved:</h2>

      <ul>
        <% security.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :security_simbol, style: "display: block" %>
    <%= form.text_field :security_simbol %>
  </div>

  <div>
    <%= form.label :creation_date, style: "display: block" %>
    <%= form.date_field :creation_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
