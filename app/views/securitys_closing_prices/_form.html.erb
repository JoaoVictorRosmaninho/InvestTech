<%= form_with(model: securitys_closing_price) do |form| %>
  <% if securitys_closing_price.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(securitys_closing_price.errors.count, "error") %> prohibited this securitys_closing_price from being saved:</h2>

      <ul>
        <% securitys_closing_price.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :closing_price, style: "display: block" %>
    <%= form.text_field :closing_price %>
  </div>

  <div>
    <%= form.label :date_closing, style: "display: block" %>
    <%= form.date_field :date_closing %>
  </div>

  <div>
    <%= form.label :security_id, style: "display: block" %>
    <%= form.number_field :security_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
